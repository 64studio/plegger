#!/usr/bin/make -f
%:
	dh $@ --with=systemd

override_dh_installinit:
	dh_systemd_enable -pplegger --name=plegger plegger.service
	dh_installinit -pplegger --no-start --noscripts
	dh_systemd_start -pplegger --no-restart-on-upgrade

override_dh_systemd_start:
	echo "Not running dh_systemd_start"
